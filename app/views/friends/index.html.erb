<div class="container my-2">
  <div class="row justify-content-center">
    <div class="col-12">
      <h5>My friends</h5>
      <% @friends.each do |friend| %>
        <%= render "profiles/profile", user: friend %>
      <% end %>
    </div>
  </div>
</div>
